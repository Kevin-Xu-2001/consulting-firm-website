import React from 'react';
import { Container } from '@mui/material';

export const Home = () => {
  console.log('home page rendered');
  return <Container>home placeholder</Container>;
};

export default Home;
